public double soma1SobreN (double x, double y) {
        if (x < y)
            x = x + 1/i;
            return soma1SobreN(x, y);

        return x;
    }

    public String invertePalavra (String palavra) {
        if (palavra.length() == 1) {
            return palavra;
        } else {
            return invertePalavra(palavra.substring(1) + palavra.charAt(0));
        }
    }

    public double somaSequencia (double a, double b) {
        if (a == 1){
            return
        }
    }
